本次会议分享了多个关于逻辑表达式自动化数据集生成与模型训练的实验结果，重点讨论了数据集划分、模型优化及项目的协作模式。

## 小结
**1. 数据集生成与使用**
- @@(144115216605901413)@@ 提供了包含训练和测试集的CSV数据文件，用于中缀表达式的重言式判定。
- @@(144115224378258316)@@ 使用逆波兰表示法（RPN）生成数据，并发现重言式出现概率约为27%，可能影响模型学习。
- @@(16802000000013609614911)@@ 分享了其使用Transformer-Encoder搭建模型的经验，指出该模型在新数据上表现不佳。

**2. 模型训练与效果分析**
- @@(16802000000013609614911)@@ 报告了使用全连接网络（FCN）和LSTM模型的结果：
    - 在自编数据上表现良好，但存在严重过拟合，以及不能泛化至新数据的问题。
    - LSTM在子串法生成的数据集上，测试集准确率约为95%，优于全连接网络。
- 当前最理想的模型期望在训练集上表现接近完美，在测试集上也应超过95%。

**3. 项目协作与规范**
- 明确了数据集需平衡重言式与非重言式样本的比例，例如各保留50%。
- 讨论并决定将后续作业核心方向定为使用Decoder的模型，并由各小组同学自行分组协作完成。

## 待办
**1. 代码分享**
- @@(16802000000013609614911)@@ 需将在实验中使用的代码分享到群里。

**2. 数据集与代码管理**
- @@(144115224378258316)@@ 需将数据集中重言式与非重言式样本的数量调整至均衡（如各50%），以应用随机采样的方式生成数据集。
- @@(144115216605901413)@@ 初步提议创建GitHub仓库来统一管理和协作，后续邮寄方案沟通。